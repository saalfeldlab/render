This directory is used by the embedded mongo library which is used for testing.
Although the files created by the library do not need to be version controlled,
this file is so that git will create the parent directory. 
